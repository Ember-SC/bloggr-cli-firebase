<div class="container-fluid">
    <div class="row-fluid">
        <div class="span3">
            <table class='table'>
                <thead>
                    <tr>
                        <th>Authors  <button class="new" {{action 'new'}}>New</button></th>
                    </tr>
                </thead>
                {{#each model}}
                    <tr><td>
                        {{#link-to 'author' this}}{{name}} {{/link-to}}
                    </td></tr>
                {{/each}}
            </table>
        </div>
        <div class="span9">
            {{outlet}}
        </div>
    </div>
</div>
