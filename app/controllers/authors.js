export default Ember.ArrayController.extend({

   actions: { 
        new: function() {
            // this.store.createRecord("author") {
            //     this.set("name", "somename");
            // }
        }
    }
});
